export const serve =  (port:number,filename:string,dir:string) => {
    console.log('Server is listening on',port);
    console.log('Saving/Fetching fcells from',filename);
    console.log('File is in directory',dir);

}